[package]
name = "maw-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for maw WASM command plugins"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
